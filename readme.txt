git init
git add readme.txt
git commit -m "git command"
git status 						
git log 						

git config --global user.name "Scott Chacon"
git config --global user.email "schacon@gmail.com"

git diff --cached

git clone git@github.com:git-hub-username/repository
git clone git@github.com:lvshaobo/repository



git push origin master

